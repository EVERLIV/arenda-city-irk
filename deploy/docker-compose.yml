services:
  web:
    build:
      context: ..
      dockerfile: Dockerfile
    image: arenda-city-web:latest
    container_name: arenda-city-web
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:3000:3000"
